// Your code here...
#include <stdio.h>

int main() {
    int number;
    scanf("%d", &number);
    
    if (is_prime(number)) {
        printf("%d\n", number);
    } else {
        printf("%d\n", number);
    }
    
    return 0;
}